ID: mbttest
version: 0.0.1
_schema-version: '3.1'
modules:
  - name: mbt-srv
    type: nodejs
    path: srv
    build-parameters:
      builder: custom
      commands: []
    provides:
      - name: srv_api
        properties:
          url: '${default-url}'